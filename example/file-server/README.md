# File server example for bunrouter

This example serves static files using `http.FileServer`. To run this example:

```go
go run main.go
```

See [documentation](https://bunrouter.uptrace.dev/) for details.
